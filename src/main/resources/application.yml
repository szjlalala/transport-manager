spring:
  profiles:
    active: dev
  jpa:
    database : MYSQL
    show-sql : true
    hibernate:
      ddl-auto : update
      naming-strategy : org.hibernate.cfg.ImprovedNamingStrategy


# log default level
logging:
  level:
    root: info

